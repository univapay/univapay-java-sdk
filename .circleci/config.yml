version: 2.1

executors:
  java8:
    docker:
      - image: circleci/openjdk:8-jdk
orbs:
  gradle: circleci/gradle@2.0.1

workflows:
  checkout-build-test:
    jobs:
      - gradle/test:
          executor: java8